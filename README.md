# 쓰는 법
## mitmproxy로 network activity를 기록하며 진행합니다
- 기기 등록할 때 request header에서 XXXX-XXXX-XXXX-XXXX 형식으로 된 auth 값을 찾습니다
- 마찬가지로 api key를 찾습니다
- homebridge json config에 auth 값을 "auth"에 api key를 "apiKey"에 대입합니다
